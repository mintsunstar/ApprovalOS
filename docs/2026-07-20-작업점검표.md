# ApprovalOS 작업 점검표

- 작성일: 2026-07-20
- 기준 커밋: `a72dca0` (`main` / `origin/main` 동기화)
- 현재 모드: **로컬 데모** (localStorage + IndexedDB). Supabase는 스키마·Edge 초안만 존재, 프론트 미연결

---

## 1. 내가(사용자) 해야 할 일

계정·서비스·브라우저·도메인처럼 **사람/외부 서비스 권한이 필요한 작업**입니다.

### A. 배포·접속 확인 (우선)

| # | 할 일 | 확인 방법 |
|---|--------|-----------|
| 1 | Vercel 최신 배포 성공 여부 확인 | Vercel Dashboard → Deployments에서 `a72dca0` 이후 빌드 Ready |
| 2 | 배포 URL에서 루트(`/`) 접속 | 랜딩이 보이면 OK. `404: NOT_FOUND`면 Output Directory=`dist`, Framework=Vite 확인 |
| 3 | 클라이언트 라우트 새로고침 | `/login`, `/dashboard` 직접 URL·F5 → 404 없어야 함 (`vercel.json` rewrite) |
| 4 | 로컬 데모 스모크 테스트 | 개발자 입장 → 프로젝트 → 시안 업로드 2장 이상 → 투표/댓글/승인 흐름 |
| 5 | (선택) localStorage quota 정리 | 예전 base64 데이터가 남아 있으면 DevTools → Application → Local Storage에서 `approvalos_db_v1` 삭제 후 새로고침 |

### B. 프로덕션 전환 시 필요한 계정·키

| # | 할 일 | 비고 |
|---|--------|------|
| 6 | Supabase 프로젝트 생성 | Auth / DB / Storage / Edge Functions |
| 7 | `.env.local`에 `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY` 설정 | `.env.local.example` 참고. **비밀키는 git에 올리지 말 것** |
| 8 | 마이그레이션 `001`–`018` 적용 | Supabase SQL Editor 또는 CLI |
| 9 | Storage 버킷 `designs` 생성·정책 확인 | `017_storage_policies.sql` |
| 10 | Edge Functions 배포 + 시크릿 | `CLAUDE_API_KEY`, 이메일 제공자(Resend 등) |
| 11 | Vercel 환경변수 등록 | 프론트용 anon 키만. service role은 Edge에만 |
| 12 | (선택) 커스텀 도메인 연결 | Vercel Domains |
| 13 | (후순위) 결제·플랜 | Stripe 등 — 현재 UI만 “후속 버전” |

### C. 제품/기획 결정 (개발 전에 정해두면 좋음)

| # | 결정 항목 |
|---|-----------|
| 14 | 데모만 유지할지, Supabase 연동 일정을 잡을지 |
| 15 | 실 AI 분석(Claude) 사용 여부·비용 |
| 16 | 실 이메일 초대/승인 알림 사용 여부 |
| 17 | 멀티 디바이스·멀티 유저가 필수인지 (필수면 localDB 한계 → 백엔드 필수) |

---

## 2. AI(에이전트)가 확인·수행할 수 있는 일

코드베이스·로컬 터미널·Git 범위에서 **직접 점검·수정 가능한 항목**입니다.

### A. 바로 확인 가능 (요청 시)

| # | 항목 | 방법 |
|---|------|------|
| 1 | `git status` / `main`·원격 동기화 | 커밋·푸시 상태 |
| 2 | `npm run build` / lint | 타입·번들 오류 |
| 3 | 라우트·페이지 존재 여부 | `src/App.tsx`, `src/pages/*` |
| 4 | 데모 데이터 경로 | `localDb.ts` ↔ `fileStore.ts`(IndexedDB) |
| 5 | Vercel SPA 설정 파일 | `vercel.json` rewrite 존재 |
| 6 | 한글 깨짐 잔여 | 페이지 문자열 / 복구 스크립트(`scripts/fix-*.mjs`) |
| 7 | PublicVote 이미지·의견란 연동 | `StoredImage`, `comment` 필드 |
| 8 | Supabase 미사용 지점 | `isSupabaseConfigured` 미호출, mock AI만 사용 등 |
| 9 | 마이그레이션·Edge stub 목록 | `supabase/migrations`, `supabase/functions` |
| 10 | 초대/가입 쿼리 미처리 등 코드 갭 | `InviteAccept`, Signup `?invite=` |

### B. 구현 가능 (요청 시 진행)

| # | 작업 |
|---|------|
| 11 | 초대 토큰 / `?invite=` 가입 연동 보완 |
| 12 | 승인 센터 탭 필터 정리 |
| 13 | AI: 설정 시 Edge 호출 + 실패 시 mock fallback |
| 14 | Supabase Auth·CRUD·Storage로 `localApi` 전환 레이어 |
| 15 | PDF/이메일 실연동 (제공자 키는 사용자 준비 필요) |
| 16 | 저장소 정리 UI (quota 안내·로컬 데이터 초기화 버튼) |
| 17 | 테스트·CI 초안 |

### C. AI가 대신 못 하는 일

| # | 항목 | 이유 |
|---|------|------|
| — | Vercel/Supabase/GitHub 콘솔 로그인·결제 | 계정 권한 |
| — | 실 서비스 API 키 발급·과금 동의 | 사용자 소유 |
| — | 배포 URL에서의 실제 클릭 UX 최종 판정 | 브라우저·네트워크·권한 (요청 시 로컬 빌드/코드 검증은 가능) |
| — | 프로덕션 데이터 삭제·실사용자 메일 발송 | 위험·권한 |

---

## 3. 현재 완료된 것 (참고)

- Phase 1–7 MVP UI·로컬 워크플로우 (시안·투표·핀·비교·AI mock·승인·보고서 인쇄)
- 개발자 원클릭 입장 (`developer@approvalos.dev` / `developer`)
- 시안 IndexedDB 저장 (localStorage quota 대응)
- Vercel SPA rewrite (`vercel.json`)
- 주요 페이지 한글 복구 + 공개 투표 이미지/의견란
- GitHub `main` 푸시됨 → Vercel 연동 시 자동 배포 대상

---

## 4. 오늘 추천 순서

1. **사용자**: Vercel 배포 Ready + URL 스모크 (섹션 1-A)
2. **사용자**: 로컬/배포에서 시안 2장 업로드까지 확인
3. **결정**: 데모 유지 vs Supabase 연동 일정
4. **요청 시 AI**: 초대 링크 보완, Supabase 연동, AI Edge 연결 중 우선순위 작업

---

## 5. 관련 경로

| 경로 | 설명 |
|------|------|
| `README.md` | 프로젝트 소개 |
| `vercel.json` | SPA rewrite |
| `src/lib/localDb.ts` | 데모 DB |
| `src/lib/fileStore.ts` | IndexedDB 파일 |
| `supabase/` | 프로덕션 스키마·Functions 초안 |
| `.env.local.example` | 환경변수 템플릿 |
| `scripts/ko-ui-labels.json` | 한글 라벨 소스 |

이 문서는 2026-07-20 시점 스냅샷입니다. 작업이 진행되면 체크 항목을 갱신하세요.
